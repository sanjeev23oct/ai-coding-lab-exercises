{
    "name": "AI Coding Lab",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "bash /workspaces/ai-coding-lab-exercises/scripts/setup-tools.sh",
    "postStartCommand": "bash /workspaces/ai-coding-lab-exercises/scripts/welcome.sh",
    "forwardPorts": [
        3000
    ],
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash"
            },
            "extensions": []
        }
    },
    "secrets": {
        "GOOGLE_API_KEY": {
            "description": "Google AI Studio API key for Gemini CLI (free at aistudio.google.com)"
        },
        "GROQ_API_KEY": {
            "description": "Groq API key for Aider (free at console.groq.com)"
        },
        "ANTHROPIC_API_KEY": {
            "description": "Anthropic API key for Claude Code (paid)"
        },
        "OPENAI_API_KEY": {
            "description": "OpenAI API key for Codex CLI (paid)"
        },
        "DEEPSEEK_API_KEY": {
            "description": "DeepSeek API key for Aider (very cheap at platform.deepseek.com)"
        }
    }
}