/**
 * Simple math utilities — used to demonstrate the PostToolUse auto-test hook.
 * When Claude Code edits this file, the hook automatically runs npm test.
 */

export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}

// multiply is missing — Claude Code will add it in the lab exercise
