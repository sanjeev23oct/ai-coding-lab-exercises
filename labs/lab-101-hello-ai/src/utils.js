// Lab 101 — Utility functions
// Your job: implement each function so all tests pass.
// Rules: use Gemini CLI to implement these. Don't write the code yourself.

/**
 * Capitalize the first letter of a string.
 * capitalize("hello") → "Hello"
 * capitalize("") → ""
 */
export function capitalize(str) {
    throw new Error('Not implemented — ask Gemini to implement this');
}

/**
 * Reverse a string.
 * reverseString("hello") → "olleh"
 * reverseString("") → ""
 */
export function reverseString(str) {
    throw new Error('Not implemented — ask Gemini to implement this');
}

/**
 * Count the number of vowels (a, e, i, o, u) in a string (case-insensitive).
 * countVowels("hello") → 2
 * countVowels("AEIOU") → 5
 * countVowels("xyz") → 0
 */
export function countVowels(str) {
    throw new Error('Not implemented — ask Gemini to implement this');
}

/**
 * Check if a string is a palindrome (reads the same forwards and backwards).
 * Comparison is case-sensitive and includes spaces as-is.
 * isPalindrome("racecar") → true
 * isPalindrome("hello")   → false
 * isPalindrome("")        → true
 */
export function isPalindrome(str) {
    throw new Error('Not implemented — ask Gemini to implement this');
}

/**
 * Truncate a string to maxLength characters.
 * If the string is longer than maxLength, cut it and add "..." at the end.
 * The total length of the result (including "...") must not exceed maxLength.
 * truncate("Hello World", 8) → "Hello..."
 * truncate("Hi", 10)        → "Hi"
 * truncate("Hello", 5)      → "Hello"  (exactly maxLength, no truncation needed)
 */
export function truncate(str, maxLength) {
    throw new Error('Not implemented — ask Gemini to implement this');
}
